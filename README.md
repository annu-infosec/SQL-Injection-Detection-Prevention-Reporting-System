# Web-Application-Input-Security-Analyzer
A web-based SQL Injection Detection &amp; Prevention System with reporting capabilities. The project analyzes user inputs, detects SQL injection keywords, classifies risk levels, logs attack attempts, generates security reports, and includes a secure login system implemented using prepared statements to prevent SQL injection vulnerabilities.
